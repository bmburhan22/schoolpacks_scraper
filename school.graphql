query a($cursor: String) {
  site {
    category(entityId: 25) {
      products(first: 50, after: $cursor) {
        edges {
          cursor
          node {
            id
            name
            path
          }
        }
        pageInfo {
          hasNextPage
        }
      }
    }
  }
}
